# Seed Default Record-Value Key

The bundled interchange seed resources now use the default `rvKey` of `®`.
They no longer override the schema with `_rv`, keeping the hand-authored seeds
consistent with the interchange format default while retaining their compact
default-kind and record-link shorthand forms.
