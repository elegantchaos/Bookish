# Record Schema File Split

Split the standard record schema declarations into `BookishRecordKey.swift` and `BookishRecordKind.swift`. Their public APIs and stored string values are unchanged; the split makes each schema surface independently discoverable.
